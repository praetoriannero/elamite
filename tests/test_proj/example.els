module kez {
    struct Derp {
        y: u16,
    }
    const t: u32 = 42;
    static u: str = "test";
    static mut v: f64 = 10.0;
    const w: Derp = new_derp();
}

fn main() -> u8 {
    let mut x = 1;
    let mut z: u32 = 4;
    let u = &x;
    x[..10].base.clear().reverse() * z;
    x && z;
    x += 1;
    let j = false;
    let y = 0 - -1 + (2 * 3) << (4 / 5);
    let y = x + 1;
    print(x);
    x.foo().bar.new();
    return;
    return x.new();
}
