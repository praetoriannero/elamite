struct Node:
    value: i32

fn main() -> ():
    var nodes = Store[Node].new()
    let handle = nodes.insert(Node { value: 1 })
    let _ = nodes.remove(handle)
    let _ = nodes.get(handle) // BuiltinTrap.StaleHandle
