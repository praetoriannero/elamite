// `ForeignRoot` and `ForeignRootMut` retain the 0.10 compatibility contract.
// `MaybeUninit` and `CVoid` require representation or lowering hooks and are
// installed from the reviewed intrinsic catalog.
