# Native concurrency demo

This package exercises `std.thread.spawn`, transfer-copy closure capture,
repeatable joins through copied thread handles, channel publication, a
copy-based mutex update, and sequentially consistent atomic operations.
