// REGRESSION 8 — unit equality lowers without applying C `==` to a struct.
//
// SPEC 4.5: "Integral primitives, `bool`, `char`, unit, and `str` provide
// total equality, ordering, and hashing."
//
// Expected and current: compiles; prints true.
//
fn main():
    let unit: () = ()
    println(f"{unit == ()}")
