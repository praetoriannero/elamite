# Closures example

This package demonstrates Elamite's explicit-capture closures, including value
and mutable-reference captures, capture aliases, closure copying, generic
`Callable` bounds, erased callable references, and escaping closure
environments.

Run it from the repository root:

```sh
cargo run -- run examples/closures
```
